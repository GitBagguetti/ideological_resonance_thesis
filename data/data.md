# Data files:

Files in this folder represent the input data used in analysis.ipynb: filtered motif CSVs (`motifs_en_filtered.csv`, `motifs_de_filtered.csv`, `motifs_media_filtered.csv`) and NER data (`qanon_ner.csv`, `media_ner.csv`). Due to large file sizes, the data is available on request only.