# Stop words
STOP_WORDS = set(
    """
share sharing shares join joining joins follow following follows link linking links
bless blessing blesses download downloading downloads explode exploding explodes subscribe subscribing subscribes
""".split()
)